name: macOS

on:
  pull_request:
    branches:
      - testing
  push:
    branches: 
      - dev

jobs:
  build:
    strategy:
      matrix:
        os: [macOS-latest]

    runs-on: ${{ matrix.os }}

    concurrency:
        group: ${{ github.ref }}-${{ github.base_ref }}-${{ github.head_ref }}-macOS-${{ matrix.kind }}
        cancel-in-progress: true
    steps:
      - uses: actions/checkout@v3
      - uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: latest
      - name: Build
        run: |
          xmake
      - name: Search
        run: |
          find .
      - name: Package
        run: |
          cd build/macosx/x86_64/release/ && zip -r xavine-macos.zip * && mv xavine-macos.zip ../../../../
      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: |
            xavine-macos.zip
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true
          generateReleaseNotes: true
          name: test
#          name: ${{ github.event.pull_request.title }}
          omitBodyDuringUpdate: true
          prerelease: true
          replacesArtifacts: true